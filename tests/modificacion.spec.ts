import 'mocha';
import {expect} from 'chai';
import {PrintableCollection} from '../src/modificacion/printableCollection';
import { NumericPrintableCollection } from '../src/modificacion/numericPrintableColecction';

/* const collection = new PrintableCollection([1, 2, 3, 4, 5]);

describe('Pruebas de la clase collectable', ()=>{
  it('Test de instanca de la clase PrintableCollection', ()=>{
    expect(collection).not.null;
    expect(collection).to.exist;
  });
  it('Test de prueba de los metodos de la clase PrintableCollection', ()=>{
    expect(collection.getNumberOfItems()).to.be.eql(5);
    expect(collection.getItem(2)).to.be.eql(3);
  });
});
*/
